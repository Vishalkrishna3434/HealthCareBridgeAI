{
    "name": "healthbridge-ai-root",
    "private": true,
    "scripts": {
        "build": "cd frontend && npm install && npm run build -- --outDir ../dist",
        "install": "cd frontend && npm install"
    }
}