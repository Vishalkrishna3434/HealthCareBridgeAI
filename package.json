{
    "name": "healthbridge-ai-root",
    "private": true,
    "scripts": {
        "build": "npm run build --prefix frontend -- --outDir ../dist",
        "install": "npm install --prefix frontend"
    }
}