# HealthBridge AI Environment Configuration
# Copy this file to .env and fill in the values

# --- AI & External APIs ---
# Required for clinical note analysis and drug interactions
GOOGLE_API_KEY=your_gemini_api_key_here

# Optional: Required for real OCR (otherwise uses mock)
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/your/service-account.json

# --- Service URLs (Backend) ---
# Used by frontend to route requests
VITE_PATIENT_SERVICE_URL=http://localhost:8080
VITE_CLINICAL_SERVICE_URL=http://localhost:8081
VITE_AI_SERVICE_URL=http://localhost:8082

# --- Firestore / Database ---
# For MCP/Mock mode, these aren't strictly required but good for configuration
FIREBASE_PROJECT_ID=healthbridge-ai-demo

# --- Pub/Sub ---
# Topic for adherence events
ADHERENCE_TOPIC=medication-adherence-events

# --- Docker ---
# Port mapping for docker-compose
FRONTEND_PORT=3000
PATIENT_SERVICE_PORT=8080
CLINICAL_SERVICE_PORT=8081
AI_SERVICE_PORT=8082
